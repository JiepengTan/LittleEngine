#pragma once
{{#include_headfiles}}
#include "{{headfile_name}}"
{{/include_headfiles}}

namespace LittleEngine{
    using namespace LittleEngine;
    {{#class_defines}}template<>
    Json LittleEngine::JsonSerializer::Write(const {{class_name}}& instance);
    template<>
    {{class_name}}& LittleEngine::JsonSerializer::Read(const Json& json_context, {{class_name}}& instance);
    {{/class_defines}}
}//namespace
